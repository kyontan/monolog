{
  "name": "monolog",
  "version": "0.0.1",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "eleventy --input=src --output=_site",
    "serve": "eleventy --input=src --output=_site --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6"
  }
}
